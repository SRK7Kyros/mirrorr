# Development mode (bun run dev): same-origin behind the Vite proxy.
# vite.config.ts forwards /api/… to http://127.0.0.1:8000/; the derived
# WebSocket origin is the page origin, so /ws/… is proxied too.
VITE_API_URL=/api
