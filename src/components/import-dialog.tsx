import { useState, useEffect } from "react";import { useQueryClient } from "@tanstack/react-query";import {    Dialog,    DialogContent,    DialogHeader,    DialogTitle,    DialogFooter,} from "@/components/ui/dialog";import { Button } from "@/components/ui/button";import { Label } from "@/components/ui/label";import {    Select,    SelectContent,    SelectItem,    SelectTrigger,    SelectValue,} from "@/components/ui/select";import {    Collapsible,    CollapsibleTrigger,    CollapsibleContent,} from "@/components/ui/collapsible";import { ScrollArea } from "@/components/ui/scroll-area";import { Spinner } from "@/components/ui/spinner";import { toast } from "sonner";import { importExportApi } from "@/lib/api";import {    ChevronRight,    Package,    CalendarClock,    Cpu,    Zap,    CheckCircle2,    AlertTriangle,    Info,    Copy,    Check,} from "lucide-react";import { cn } from "@/lib/utils";import { AREAS, PLUGIN_CELLS, TIME_RANGE } from "@/lib/layouts";type PluginRef = { name: string; origin_hash: string };interface Issue {    field?: string;    problem: string;    bundled?: PluginRef;    installed?: PluginRef;    alternatives?: Array<{ id: number; name: string; origin_hash: string }>;    existing_id?: number;    profile_name?: string;    new_name?: string;}interface ProfileResult {    name: string;    content_hash: string;    valid: boolean;    issues: Issue[];}interface AutorunResult {    name: string;    valid: boolean;    issues: Issue[];}interface ValidationReport {    valid: boolean;    profiles: ProfileResult[];    autoruns: AutorunResult[];}type Phase = "loading" | "confirm" | "resolve";export function ImportDialog({    open,    onClose,    onImported,    bundle,}: {    open: boolean;    onClose: () => void;    onImported: () => void;    bundle: Record<string, unknown> | null;}) {    const queryClient = useQueryClient();    const [phase, setPhase] = useState<Phase>("loading");    const [report, setReport] = useState<ValidationReport | null>(null);    const [pluginMap, setPluginMap] = useState<        Record<string, { type: string; id: number }>    >({});    const [importing, setImporting] = useState(false);    useEffect(() => {        if (!open || !bundle) return;        setPhase("loading");        setPluginMap({});        importExportApi            .validate(bundle)            .then((data) => {                setReport(data as unknown as ValidationReport);                const r = data as unknown as ValidationReport;                const initialMap: Record<string, { type: string; id: number }> =                    {};                for (const p of r.profiles) {                    for (const issue of p.issues) {                        if (issue.bundled && issue.alternatives?.length) {                            const best =                                issue.alternatives.find(                                    (a) => a.name === issue.bundled!.name,                                ) ?? issue.alternatives[0];                            initialMap[issue.bundled.origin_hash] = {                                type: issue.field ?? "",                                id: best.id,                            };                        }                    }                }                for (const a of r.autoruns) {                    for (const issue of a.issues) {                        if (issue.bundled && issue.alternatives?.length) {                            const best =                                issue.alternatives.find(                                    (al) => al.name === issue.bundled!.name,                                ) ?? issue.alternatives[0];                            initialMap[issue.bundled.origin_hash] = {                                type: issue.field ?? "",                                id: best.id,                            };                        }                    }                }                setPluginMap(initialMap);                setPhase(r.valid ? "confirm" : "resolve");            })            .catch((err) => {                toast.error(`Validation failed: ${err.message}`);                onClose();            });    }, [open, bundle]);    function allResolved(): boolean {        if (!report) return false;        for (const p of report.profiles) {            for (const issue of p.issues) {                if (                    issue.problem === "name_conflict" ||                    issue.problem === "already_exists"                )                    continue;                if (issue.bundled && !pluginMap[issue.bundled.origin_hash])                    return false;            }        }        for (const a of report.autoruns) {            for (const issue of a.issues) {                if (issue.problem === "profile_missing") return false;                if (issue.bundled && !pluginMap[issue.bundled.origin_hash])                    return false;            }        }        return true;    }    async function handleImport() {        if (!bundle) return;        setImporting(true);        try {            const result = await importExportApi.apply(bundle, pluginMap);            const msgs: string[] = [];            if (result.profiles_created)                msgs.push(`${result.profiles_created} profile(s) created`);            if (result.autoruns_created)                msgs.push(`${result.autoruns_created} autorun(s) created`);            if (result.profiles_skipped)                msgs.push(                    `${result.profiles_skipped} profile(s) skipped (identical)`,                );            toast.success(msgs.length ? msgs.join(", ") : "Import complete");            queryClient.invalidateQueries({ queryKey: ["profiles"] });            queryClient.invalidateQueries({ queryKey: ["autoruns"] });            onImported();            onClose();        } catch (err: unknown) {            const msg = err instanceof Error ? err.message : "Import failed";            toast.error(msg);        } finally {            setImporting(false);        }    }    function setPluginMapping(hash: string, type: string, id: number) {        setPluginMap((prev) => ({ ...prev, [hash]: { type, id } }));    }    const profiles = (bundle?.profiles as Array<Record<string, unknown>>) ?? [];    const autoruns = (bundle?.autoruns as Array<Record<string, unknown>>) ?? [];    return (        <Dialog open={open} onOpenChange={(v) => !v && onClose()}>            <DialogContent className="max-w-2xl max-h-[85vh] flex flex-col">                <DialogHeader>                    <DialogTitle className="flex items-center gap-2">                        <Package className="size-4" />                        Import Bundle                    </DialogTitle>                </DialogHeader>                <ScrollArea className="flex-1 min-h-0 pr-1">                    <div className="space-y-3">                        {phase === "loading" && (                            <div className="flex items-center justify-center py-10 gap-2 text-sm text-muted-foreground">                                <Spinner className="size-4" /> Validating                                bundle...                            </div>                        )}                        {(phase === "confirm" || phase === "resolve") && (                            <>                                {profiles.map((p, i) => {                                    const profileReport = report?.profiles[i];                                    const issues = profileReport?.issues ?? [];                                    const status =                                        issues.length === 0                                            ? ("ok" as const)                                            : issues.some(                                                    (i) =>                                                        i.problem ===                                                        "already_exists",                                                )                                              ? ("skip" as const)                                              : issues.some(                                                      (i) =>                                                          i.problem ===                                                          "name_conflict",                                                  )                                                ? ("rename" as const)                                                : ("issue" as const);                                    return (                                        <ProfilePreviewCard                                            key={i}                                            profile={p}                                            status={status}                                            issues={issues}                                            pluginMap={pluginMap}                                            onPluginChange={setPluginMapping}                                        />                                    );                                })}                                {autoruns.map((a, i) => {                                    const autorunReport = report?.autoruns[i];                                    const issues = autorunReport?.issues ?? [];                                    const status =                                        issues.length === 0                                            ? ("ok" as const)                                            : ("issue" as const);                                    return (                                        <AutorunPreviewCard                                            key={i}                                            autorun={a}                                            status={status}                                            issues={issues}                                            pluginMap={pluginMap}                                            onPluginChange={setPluginMapping}                                        />                                    );                                })}                            </>                        )}                    </div>                </ScrollArea>                <DialogFooter>                    <Button                        variant="ghost"                        size="sm"                        className="h-7 text-xs"                        onClick={onClose}                    >                        Cancel                    </Button>                    {(phase === "confirm" || phase === "resolve") && (                        <Button                            size="sm"                            className="h-7 text-xs"                            onClick={handleImport}                            disabled={                                importing ||                                (phase === "resolve" && !allResolved())                            }                        >                            {importing && <Spinner className="size-3 mr-1" />}                            Import                        </Button>                    )}                </DialogFooter>            </DialogContent>        </Dialog>    );}// ── Profile Preview Card ────────────────────────────────────────function ProfilePreviewCard({    profile,    status,    issues,    pluginMap,    onPluginChange,}: {    profile: Record<string, unknown>;    status: "ok" | "skip" | "rename" | "issue";    issues: Issue[];    pluginMap: Record<string, { type: string; id: number }>;    onPluginChange: (hash: string, type: string, id: number) => void;}) {    const [open, setOpen] = useState(false);    const name = profile.name as string;    const engine = profile.default_engine as PluginRef | undefined;    const resolver = profile.resolver as PluginRef | undefined;    const resolverConfig = profile.resolver_config as        | Record<string, unknown>        | undefined;    const retryMode = (profile.retry_mode as string) ?? "none";    const retryConfig = profile.retry_config as        | Record<string, unknown>        | undefined;    const contentHash = profile.content_hash as string | undefined;    const sc = {        ok: {            color: "border-emerald-500/30",            bg: "bg-emerald-500/5",            Icon: CheckCircle2,            ic: "text-emerald-500",            label: "Ready to import",        },        skip: {            color: "border-emerald-500/30",            bg: "bg-emerald-500/5",            Icon: Info,            ic: "text-emerald-500",            label: "Already exists — will be skipped",        },        rename: {            color: "border-blue-500/30",            bg: "bg-blue-500/5",            Icon: AlertTriangle,            ic: "text-blue-500",            label: `Will be renamed to ${issues.find((i) => i.new_name)?.new_name ?? `${name}_2`}`,        },        issue: {            color: "border-amber-500/30",            bg: "bg-amber-500/5",            Icon: AlertTriangle,            ic: "text-amber-500",            label: "Needs resolution",        },    }[status];    return (        <Collapsible open={open} onOpenChange={setOpen}>            <div                className={cn("rounded-lg border transition-colors", sc.color)}            >                <CollapsibleTrigger className="w-full">                    <div className={cn("px-4 py-3", sc.bg)}>                        <div className="flex items-center gap-3">                            <ChevronRight                                className={cn(                                    "size-4 shrink-0 transition-transform text-muted-foreground",                                    open && "rotate-90",                                )}                            />                            {/* Row 1: Name left, badge right */}                            <div className="flex-1 min-w-0 text-left">                                <p className="text-sm font-medium truncate">                                    {name}                                </p>                            </div>                            <div className="flex items-center gap-1.5 shrink-0">                                <sc.Icon className={cn("size-3.5", sc.ic)} />                                <span className="text-xs text-muted-foreground">                                    {sc.label}                                </span>                            </div>                        </div>                        {/* Row 2: Engine + Resolver */}                        <div className="flex items-center gap-4 ml-7 mt-1.5">                            <div className="flex items-center gap-1.5 text-xs text-muted-foreground">                                <Cpu className="size-3" />                                <span>{engine?.name ?? "?"}</span>                            </div>                            <div className="flex items-center gap-1.5 text-xs text-muted-foreground">                                <Zap className="size-3" />                                <span>{resolver?.name ?? "?"}</span>                            </div>                            {retryMode !== "none" && (                                <span className="text-xs text-muted-foreground">                                    Retry: {retryMode}                                </span>                            )}                        </div>                    </div>                </CollapsibleTrigger>                <CollapsibleContent>                    <div className="px-4 pb-4 pt-2 space-y-3 border-t">                        {/* Plugins grid */}                        <div className="grid gap-3" style={PLUGIN_CELLS.style}>                            <div style={{ gridArea: AREAS.engine }}>                                <PluginCell                                    icon={Cpu}                                    label="Engine"                                    plugin={engine}                                />                            </div>                            <div style={{ gridArea: AREAS.resolver }}>                                <PluginCell                                    icon={Zap}                                    label="Resolver"                                    plugin={resolver}                                />                            </div>                        </div>                        {/* Resolver config */}                        {resolverConfig &&                            Object.keys(resolverConfig).length > 0 && (                                <ConfigPreview                                    title="Resolver Config"                                    config={resolverConfig}                                />                            )}                        {/* Retry config — includes mode as a row */}                        {retryMode !== "none" &&                            retryConfig &&                            Object.keys(retryConfig).length > 0 && (                                <ConfigPreview                                    title={`Retry Config (${retryMode})`}                                    config={retryConfig}                                />                            )}                        {/* Content hash */}                        {contentHash && (                            <p className="text-2xs text-muted-foreground font-mono">                                Hash: {contentHash}                            </p>                        )}                        {/* Issues */}                        {issues.map((issue, i) => (                            <IssueRow                                key={i}                                issue={issue}                                pluginMap={pluginMap}                                onChange={onPluginChange}                            />                        ))}                    </div>                </CollapsibleContent>            </div>        </Collapsible>    );}// ── Autorun Preview Card ────────────────────────────────────────function AutorunPreviewCard({    autorun,    status,    issues,    pluginMap,    onPluginChange,}: {    autorun: Record<string, unknown>;    status: "ok" | "issue";    issues: Issue[];    pluginMap: Record<string, { type: string; id: number }>;    onPluginChange: (hash: string, type: string, id: number) => void;}) {    const [open, setOpen] = useState(false);    const name = autorun.user_friendly_name as string;    const startTime = autorun.start_time as string;    const endTime = autorun.end_time as string;    const recording = autorun.recording as boolean;    const profileName = autorun.profile_name as string | undefined;    const engineOverride = autorun.engine_override as PluginRef | undefined;    const contentHash = autorun.content_hash as string | undefined;    const sc =        status === "ok"            ? {                  color: "border-emerald-500/30",                  bg: "bg-emerald-500/5",                  Icon: CheckCircle2,                  ic: "text-emerald-500",                  label: "Ready to import",              }            : {                  color: "border-amber-500/30",                  bg: "bg-amber-500/5",                  Icon: AlertTriangle,                  ic: "text-amber-500",                  label: "Needs resolution",              };    return (        <Collapsible open={open} onOpenChange={setOpen}>            <div                className={cn("rounded-lg border transition-colors", sc.color)}            >                <CollapsibleTrigger className="w-full">                    <div                        className={cn(                            "flex items-center gap-3 px-4 py-3",                            sc.bg,                        )}                    >                        <ChevronRight                            className={cn(                                "size-4 shrink-0 transition-transform text-muted-foreground",                                open && "rotate-90",                            )}                        />                        <CalendarClock className="size-4 shrink-0 text-muted-foreground" />                        <div className="flex-1 min-w-0 text-left">                            <p className="text-sm font-medium truncate">                                {name}                            </p>                            <p className="text-xs text-muted-foreground truncate">                                Profile: {profileName ?? "?"} ·{" "}                                {recording ? "Recording" : "Stream only"}                            </p>                        </div>                        <div className="flex items-center gap-1.5 shrink-0">                            <sc.Icon className={cn("size-3.5", sc.ic)} />                            <span className="text-xs text-muted-foreground">                                {sc.label}                            </span>                        </div>                    </div>                </CollapsibleTrigger>                <CollapsibleContent>                    <div className="px-4 pb-4 pt-1 space-y-3 border-t">                        <div className="grid gap-3" style={TIME_RANGE.style}>                            <div className="space-y-1" style={{ gridArea: AREAS.start }}>                                <Label className="text-2xs text-muted-foreground uppercase tracking-wider">                                    Start                                </Label>                                <p className="text-xs">                                    {startTime                                        ? new Date(startTime).toLocaleString(                                              undefined,                                              { hour12: false },                                          )                                        : "—"}                                </p>                            </div>                            <div className="space-y-1" style={{ gridArea: AREAS.end }}>                                <Label className="text-2xs text-muted-foreground uppercase tracking-wider">                                    End                                </Label>                                <p className="text-xs">                                    {endTime                                        ? new Date(endTime).toLocaleString(                                              undefined,                                              { hour12: false },                                          )                                        : "—"}                                </p>                            </div>                        </div>                        {engineOverride && (                            <PluginCell                                icon={Cpu}                                label="Engine Override"                                plugin={engineOverride}                            />                        )}                        {contentHash && (                            <p className="text-2xs text-muted-foreground font-mono">                                Hash: {contentHash}                            </p>                        )}                        {issues.map((issue, i) => (                            <IssueRow                                key={i}                                issue={issue}                                pluginMap={pluginMap}                                onChange={onPluginChange}                            />                        ))}                    </div>                </CollapsibleContent>            </div>        </Collapsible>    );}// ── Plugin Cell (hover-to-reveal hash, click-to-copy) ───────────function PluginCell({    icon: Icon,    label,    plugin,}: {    icon: React.ComponentType<{ className?: string }>;    label: string;    plugin: PluginRef | undefined;}) {    const [hovered, setHovered] = useState(false);    const [copied, copy] = useCopyToClipboard();    function handleClick() {        if (!plugin?.origin_hash) return;        copy(plugin.origin_hash);    }    return (        <div            className="flex items-center gap-2 rounded-md border px-2.5 py-2 bg-muted/10 hover:bg-muted/20 transition-colors cursor-pointer select-none"            onMouseEnter={() => setHovered(true)}            onMouseLeave={() => {                setHovered(false);                setCopied(false);            }}            onClick={handleClick}            title={plugin?.origin_hash ? "Click to copy hash" : undefined}        >            <Icon className="size-3.5 text-muted-foreground shrink-0" />            <div className="flex-1 min-w-0">                <p className="text-2xs text-muted-foreground uppercase tracking-wider">                    {label}                </p>                <div className="flex items-center gap-1">                    {/* Name (default) */}                    <p                        className={cn(                            "text-xs font-medium truncate transition-all duration-200",                            hovered ? "opacity-0 w-0" : "opacity-100",                        )}                    >                        {plugin?.name ?? "?"}                    </p>                    {/* Hash (on hover) */}                    <p                        className={cn(                            "text-2xs font-mono text-muted-foreground transition-all duration-200 flex items-center gap-1 min-w-0",                            hovered ? "opacity-100" : "opacity-0 w-0",                        )}                    >                        {copied ? (                            <>                                <Check className="size-3 shrink-0 text-emerald-500" />                                Copied!                            </>                        ) : (                            <>                                <span className="truncate">                                    {plugin?.origin_hash?.slice(0, 16) + "…" ??                                        "?"}                                </span>                                <Copy className="size-3 shrink-0 opacity-50" />                            </>                        )}                    </p>                </div>            </div>        </div>    );}// ── Config Preview (key-value table) ────────────────────────────function ConfigPreview({    title,    config,}: {    title: string;    config: Record<string, unknown>;}) {    const entries = Object.entries(config);    if (entries.length === 0) return null;    return (        <div className="space-y-1">            <Label className="text-2xs text-muted-foreground uppercase tracking-wider">                {title}            </Label>            <div className="rounded border bg-muted/20 overflow-hidden divide-y divide-border/50">                {entries.map(([key, value]) => (                    <div                        key={key}                        className="flex items-center gap-3 py-1.5 px-3"                    >                        <code className="font-mono font-semibold text-foreground text-xs shrink-0">                            {key}                        </code>                        <ConfigValue                            value={                                typeof value === "string"                                    ? value                                    : JSON.stringify(value)                            }                        />                    </div>                ))}            </div>        </div>    );}function ConfigValue({ value }: { value: string }) {    const [hovered, setHovered] = useState(false);    const [copied, copy] = useCopyToClipboard();    const needsTruncate = value.length > 40;    function handleClick() {        copy(value);    }    if (!needsTruncate) {        return (            <code className="text-xs font-mono text-muted-foreground text-right flex-1 min-w-0">                {value}            </code>        );    }    return (        <div            className="flex-1 min-w-0 flex items-center justify-end gap-1 cursor-pointer select-none"            onMouseEnter={() => setHovered(true)}            onMouseLeave={() => {                setHovered(false);                setCopied(false);            }}            onClick={handleClick}            title="Click to copy"        >            <code                className={cn(                    "text-xs font-mono text-muted-foreground truncate min-w-0 text-right transition-all duration-200",                    hovered && "text-foreground",                )}            >                {copied ? "Copied!" : value}            </code>            <span                className={cn(                    "shrink-0 transition-all duration-200",                    hovered ? "opacity-100" : "opacity-0",                )}            >                {copied ? (                    <Check className="size-3 text-emerald-500" />                ) : (                    <Copy className="size-3 text-muted-foreground opacity-50" />                )}            </span>        </div>    );}// ── Issue Row ───────────────────────────────────────────────────function IssueRow({    issue,    pluginMap,    onChange,}: {    issue: Issue;    pluginMap: Record<string, { type: string; id: number }>;    onChange: (hash: string, type: string, id: number) => void;}) {    if (issue.problem === "already_exists") {        return (            <div className="flex items-center gap-2 rounded border border-emerald-500/30 bg-emerald-500/5 px-3 py-2 text-xs text-emerald-700 dark:text-emerald-300">                <CheckCircle2 className="size-3.5 shrink-0" />                <span>Identical content already exists — will be skipped.</span>            </div>        );    }    if (issue.problem === "name_conflict") {        return (            <div className="flex items-center gap-2 rounded border border-blue-500/30 bg-blue-500/5 px-3 py-2 text-xs text-blue-700 dark:text-blue-300">                <AlertTriangle className="size-3.5 shrink-0" />                <span>                    Name conflict — will be imported as:{" "}                    <strong>                        {issue.new_name ?? `${issue.bundled?.name}_2`}                    </strong>                </span>            </div>        );    }    if (issue.problem === "profile_missing") {        return (            <div className="flex items-center gap-2 rounded border border-red-500/30 bg-red-500/5 px-3 py-2 text-xs text-red-700 dark:text-red-300">                <AlertTriangle className="size-3.5 shrink-0" />                <span>                    References profile <strong>{issue.profile_name}</strong>{" "}                    which is not available.                </span>            </div>        );    }    if (!issue.bundled) return null;    const currentHash = issue.bundled.origin_hash;    const currentSelection = pluginMap[currentHash];    return (        <div className="rounded border px-3 py-2.5 space-y-2">            <div className="flex items-center justify-between">                <span className="text-xs font-medium">                    {issue.bundled.name}                </span>                <span className="text-2xs text-muted-foreground font-mono truncate max-w-[120px]">                    {currentHash}                </span>            </div>            <p className="text-xs text-muted-foreground">                {issue.problem === "not_installed"                    ? "Not installed"                    : "Hash mismatch — plugin was updated"}            </p>            {issue.alternatives && issue.alternatives.length > 0 && (                <div className="space-y-1">                    <Label className="text-2xs">Map to:</Label>                    <Select                        value={                            currentSelection ? String(currentSelection.id) : ""                        }                        onValueChange={(v) =>                            onChange(                                currentHash,                                issue.field ?? "",                                parseInt(v),                            )                        }                    >                        <SelectTrigger className="h-7 text-xs">                            <SelectValue placeholder="Select plugin" />                        </SelectTrigger>                        <SelectContent>                            {issue.alternatives.map((alt) => (                                <SelectItem key={alt.id} value={String(alt.id)}>                                    {alt.name}{" "}                                    <span className="text-2xs text-muted-foreground ml-1">                                        {alt.origin_hash.slice(0, 8)}…                                    </span>                                </SelectItem>                            ))}                        </SelectContent>                    </Select>                </div>            )}            {issue.alternatives && issue.alternatives.length === 0 && (                <p className="text-xs text-destructive">                    No plugins available to resolve this.                </p>            )}        </div>    );}